/* Table of Theme Benchmark Items Frequencies */


$("document").ready(function() {

    $(".view115b").each(function() {
        tableChartCoreFixedLabel($(this));
        tableChartExport($(this));
        tableChartScroll($(this));
        tableChartHead($(this));
    });

});
