/* Table of Theme Units Means */

$("document").ready(function() {

    $(".view112b").each(function() {
        tableChartCoreFixedLabel($(this));
        tableChartExport($(this));
        tableChartScroll($(this));
        tableChartHead($(this));
    });

});
